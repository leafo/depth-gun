-- Runs FIRST in the collection. Initializes top-level state via game_state.
-- All other scripts read state via game_state.get_world() / get_audio() etc.
-- — no _G.WORLD or _G.AUDIO.

local game_state = require("main.scripts.game_state")

function init(self)
    game_state.init()
end
